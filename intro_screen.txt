    {!hasStarted && (
      <div className="absolute inset-0 z-[200] flex flex-col items-center justify-center bg-black/40 backdrop-blur-md">
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          className="bg-white/10 backdrop-blur-xl border border-white/20 p-10 md:p-12 rounded-3xl shadow-2xl max-w-md w-full mx-4 text-center"
        >
          <h1 className="text-3xl md:text-4xl font-serif text-white mb-3 tracking-tight">Rain Memory</h1>
          <p className="text-white/70 text-lg mb-8 font-light italic">The rain remembers every touch.</p>
          
          <div className="space-y-6 text-left mb-10">
            <div className="flex items-start gap-4">
              <span className="text-2xl mt-1">💧</span>
              <div>
                <h3 className="text-white font-medium mb-1">Click the screen</h3>
                <p className="text-white/60 text-sm">Let raindrops fall and create ripples.</p>
              </div>
            </div>
            
            <div className="flex items-start gap-4">
              <span className="text-2xl mt-1">🥾</span>
              <div>
                <h3 className="text-white font-medium mb-1">Move your cursor</h3>
                <p className="text-white/60 text-sm">Your cursor becomes a rain boot, leaving traces on the water.</p>
              </div>
            </div>
            
            <div className="flex items-start gap-4">
              <span className="text-2xl mt-1">🌊</span>
              <div>
                <h3 className="text-white font-medium mb-1">Touch the surface</h3>
                <p className="text-white/60 text-sm">Watch memories spread like waves.</p>
              </div>
            </div>
          </div>
          
          <button 
            onClick={() => {
              setHasStarted(true);
              synth.init(); // Initialize audio context on first interaction
            }}
            className="w-full py-4 rounded-xl bg-white text-zinc-900 font-medium text-lg hover:bg-zinc-100 hover:scale-[1.02] active:scale-[0.98] transition-all shadow-lg"
          >
            Start Experience
          </button>
        </motion.div>
      </div>
    )}

  </div>
  );
};

export default StaircaseWaterway;
